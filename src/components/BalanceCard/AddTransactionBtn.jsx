import React from 'react';

const AddTransactionBtn = () => {
  return (
    <button className="w-full py-idx-2 rounded-idx-xl border border-primary-icon/30 text-primary-icon text-idx-sm font-medium hover:bg-white/40 hover:text-secondary transition-all duration-300">
      Добавить транзакцию
    </button>
  );
};

export default AddTransactionBtn;